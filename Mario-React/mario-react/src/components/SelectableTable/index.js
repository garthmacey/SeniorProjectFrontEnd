import { SelectableTable } from './SelectableTable';
import { enhance } from './SelectableTable.enhancer';

// eslint-disable-next-line import/no-default-export
export default enhance(SelectableTable);
