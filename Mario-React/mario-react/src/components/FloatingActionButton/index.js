import { FloatingActionButton } from './FloatingActionButton';
import { enhance } from './FloatingActionButton.enhancer';

// eslint-disable-next-line import/no-default-export
export default enhance(FloatingActionButton);
