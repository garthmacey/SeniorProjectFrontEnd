import { createStyles } from '@material-ui/core';

const styles = createStyles({
  primary: {
    width: '100%',
  },
  tableHeader: {
    fontWeight: '600',
    borderBottom: 'solid',
  },
  DataTable: {
    width: '100%',
  },
});

export { styles };
